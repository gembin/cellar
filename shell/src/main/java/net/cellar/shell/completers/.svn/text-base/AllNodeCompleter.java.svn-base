package com.upstreamsystems.curry.cluster.shell.completers;

import com.upstreamsystems.curry.cluster.api.Node;

/**
 * A completer which includes all nodes.
 * @author iocanel
 */
public class AllNodeCompleter extends NodeCompleterSupport {

	/**
	 * Always returns true
	 * @param node
	 * @return
	 */
	@Override
	protected boolean acceptsNode(Node node) {
		return true;
	}
}
